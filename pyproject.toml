[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "distributed-training-resume-template"
version = "0.1.0"
description = "Internal Translucid distributed training resume challenge template"
requires-python = ">=3.12"
dependencies = []
